# 长春高新经营绩效分析项目工作进度说明

## 当前状态

已完成“上市公司绩效评价与可视化展示”课程作业的 Python 项目搭建，并已将完整项目复制到桌面文件夹：

```text
C:\Users\zengb\Desktop\股票
```

桌面 `股票` 文件夹中包含：

- `company-performance-analysis`：完整 Python 项目目录
- `company-performance-analysis-project.zip`：项目压缩包

当前工作区中临时生成的项目副本、压缩包和浏览器验证缓存已删除，仅保留本进度说明文件。

## 已完成内容

1. 创建了完整项目结构，包括 `data`、`src`、`outputs`、`app.py`、`requirements.txt`、`README.md`。
2. 实现了财务数据导入能力，支持手动导入 CSV/Excel。
3. 封装了 AkShare 数据获取接口，用于获取 A 股财务指标和历史股价数据。
4. 构建了经营绩效指标体系，包括营业收入、净利润、ROE、ROA、毛利率、净利率、资产负债率、流动比率、经营现金流等指标。
5. 实现了指标正向化、标准化、熵权法权重计算和 TOPSIS 综合绩效评分。
6. 实现了图表导出功能，图表保存到项目内 `outputs/figures`。
7. 实现了 Streamlit 可视化看板，支持上传财务数据并展示绩效结果。
8. 使用教学演示数据完成了一次端到端运行验证。

## 已生成的核心输出

在桌面项目目录中已生成：

- `outputs/report_data.xlsx`：综合结果工作簿
- `outputs/tables/entropy_weights.csv`：熵权法指标权重表
- `outputs/tables/topsis_scores.csv`：TOPSIS 综合绩效得分与排名
- `outputs/figures/`：趋势图、雷达图、权重柱状图、综合得分折线图、股价与绩效对比图

## 验证情况

已验证以下命令可以运行：

```bash
python src/report_charts.py --demo
```

已验证 Streamlit 看板可以启动并访问：

```bash
streamlit run app.py
```

本次验证使用的是教学演示数据。正式提交时建议使用 AkShare 或从年报、财报平台整理出的真实长春高新财务数据。

## 后续建议

1. 在桌面 `股票\company-performance-analysis` 中继续修改和运行项目。
2. 若需要真实数据，可安装或升级 AkShare 后运行：

```bash
python src/report_charts.py --use-akshare --symbol 000661 --start-date 20190101
```

3. 若 AkShare 接口不可用，可将整理好的财务数据放入 `data/raw`，然后运行：

```bash
python src/report_charts.py --financial-file data/raw/changchun_gaoxin_financials.xlsx
```

4. 写课程报告时，需要说明示例数据不代表真实审计财报；正式分析应注明数据来源、数据日期和财务口径。
